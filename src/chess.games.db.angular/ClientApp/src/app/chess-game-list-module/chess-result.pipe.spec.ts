import { ChessResultPipe } from './chess-result.pipe';

describe('ChessResultPipe', () => {
  it('create an instance', () => {
    const pipe = new ChessResultPipe();
    expect(pipe).toBeTruthy();
  });
});
