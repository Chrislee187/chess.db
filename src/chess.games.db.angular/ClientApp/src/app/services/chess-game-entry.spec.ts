import { ChessGameItem as ChessGameEntry } from './ChessGameItem';

describe('ChessGameItem', () => {
  it('should create an instance', () => {
    expect(new ChessGameEntry()).toBeTruthy();
  });
});
